#User function Template for python3

class Solution:
    def isPerfectNumber(self, N):
        # code here 
        sum=0;
        for i in range(1,N):
            if(N%i==0):
                sum+=i
        if sum==N:
            return 1
        else:
            return 0
